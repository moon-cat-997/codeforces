# compiled binaries
a.out
*.o
*.out
sol
cf_run
/tmp/
# ...but DO track sample expected-output fixtures (they're test data, not binaries)
!problems/*/tests/*.out
# bare-named binaries left by ad-hoc `g++ -o main ...` (does NOT match main.cpp)
main
problems/*/main
# macOS debug-symbol bundles from `g++ -g` builds
*.dSYM/

# editor / OS
.vscode/*.log
.DS_Store
