{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CF: debug (stdin from tests/1.in)",
            "type": "lldb",
            "request": "launch",
            "program": "/tmp/cf_debug_bin",
            "args": [],
            "cwd": "${fileDirname}",
            "stdio": [
                "${fileDirname}/tests/1.in",
                null,
                null
            ],
            "preLaunchTask": "cf: build debug (active file)"
        },
        {
            "name": "CF: debug (type input in terminal)",
            "type": "lldb",
            "request": "launch",
            "program": "/tmp/cf_debug_bin",
            "args": [],
            "cwd": "${fileDirname}",
            "terminal": "integrated",
            "preLaunchTask": "cf: build debug (active file)"
        },
        {
            "name": "CF: debug (pick a test)",
            "type": "lldb",
            "request": "launch",
            "program": "/tmp/cf_debug_bin",
            "args": [],
            "cwd": "${fileDirname}",
            "stdio": [
                "${fileDirname}/tests/${input:testNumber}.in",
                null,
                null
            ],
            "preLaunchTask": "cf: build debug (active file)"
        }
    ],
    "inputs": [
        {
            "id": "testNumber",
            "type": "promptString",
            "description": "Which tests/<n>.in to feed into stdin?",
            "default": "1"
        }
    ]
}
